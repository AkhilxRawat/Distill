replicaCount: 1
image:
  tag: "6056736cdadd3dc6a977a86152b094c4860b0a99"
resources:
  requests:
    memory: "256Mi"
    cpu: "250m"
  limits:
    memory: "512Mi"
    cpu: "500m"

# Base values.yaml points these at *.distill-dev; in prod every service
# runs in this same namespace (distill-main), addressed by release name.
env:
  INGESTION_HOST: distill-ingestion-main
  STORAGE_HOST: distill-storage-main
