apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  labels:
    app: ingestion
spec:
  type: {{ .Values.service.type | default "ClusterIP" }}
  ports:
    - name: grpc
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
    - name: metrics
      port: 9092
      targetPort: 9092
      protocol: TCP
  selector:
    app: ingestion
