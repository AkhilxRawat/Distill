replicaCount: 2
image:
  tag: "latest" # placeholder — must be set via the promote workflow before this is used for real
resources:
  requests:
    memory: "256Mi"
    cpu: "250m"
  limits:
    memory: "512Mi"
    cpu: "500m"
