replicaCount: 1

image:
  repository: mongo
  tag: "7"
  pullPolicy: IfNotPresent

service:
  port: 27017

persistence:
  enabled: true
  size: 1Gi
  storageClassName: standard

resources:
  requests:
    memory: 256Mi
    cpu: 100m
  limits:
    memory: 512Mi
    cpu: 500m
