replicaCount: 1
image:
  tag: "6056736cdadd3dc6a977a86152b094c4860b0a99"
resources:
  requests:
    memory: "256Mi"
    cpu: "250m"
  limits:
    memory: "512Mi"
    cpu: "500m"
env:
  # Points at a distill-mongodb-main release in the distill-main namespace —
  # not deployed yet (see Step 6 / RAM discussion). storage-main will not
  # connect until that's set up, same as its current state, not a regression.
  MONGODB_URI: "mongodb://distill-mongodb-main-0.distill-mongodb-main.distill-main.svc.cluster.local:27017,distill-mongodb-main-1.distill-mongodb-main.distill-main.svc.cluster.local:27017,distill-mongodb-main-arbiter.distill-main.svc.cluster.local:27017/distill?replicaSet=rs0"
