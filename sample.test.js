describe('Sample Test Suite', () => {
  test('should verify arithmetic operations', () => {
    expect(1 + 1).toBe(2);
  });

  test('should verify string comparison', () => {
    expect('Distill').toBe('Distill');
  });
});
