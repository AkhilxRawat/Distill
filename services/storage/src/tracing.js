'use strict';

// Tracing stub — replace with OpenTelemetry setup if needed.

function initTracing(serviceName) {
  console.log(`[tracing] Tracing not configured for service: ${serviceName}`);
}

module.exports = { initTracing };